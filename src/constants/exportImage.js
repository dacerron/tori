/** JPEG quality for canvas `toBlob` (0–1). Change only here. */
export const EXPORT_JPEG_QUALITY = 0.85

/** Max output height in px; wider images scale down; no upscale. */
export const EXPORT_MAX_HEIGHT = 720
